Apache Configuration
====================

```
apt-get install libapache2-mod-wsgi

ln -s /home/datastore/sra/etc/pyramid.wsgi /home/datastore/env/pyramid.wsgi 
cp /home/datastore/sra/etc/sra /etc/apache2/sites-available/sra

a2dissite default
a2ensite sra
service apache2 reload
```
